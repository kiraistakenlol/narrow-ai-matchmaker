import { Module } from '@nestjs/common';

@Module({
    imports: [],
    controllers: [], // Add OnboardingController later
    providers: [], // Add OnboardingService later
})
export class OnboardingModule {} 